{
  "description": "Architect Agent hooks. Hook data is passed via stdin as JSON - scripts read from stdin, not env vars.",
  "hooks": {
    "Stop": [
      {
        "_id": "amaa-stop-check",
        "_description": "Blocks exit until all design work is complete",
        "hooks": [
          {
            "type": "command",
            "command": "python3",
            "args": ["${CLAUDE_PLUGIN_ROOT}/scripts/amaa_stop_check.py"],
            "timeout": 15
          }
        ]
      }
    ]
  }
}
