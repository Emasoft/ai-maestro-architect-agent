---
name: architecture
description: "how does ai-maestro-architect-agent (AMAA) work — the ARCHITECT role plugin: overview, the main parts (agents, skills, design pipeline, hooks), where the key pieces live / does this agent pin a model / why is there no model field in agent frontmatter / which model does the planner use / cache-warmth invariant CPV CA-04 / is the model pin still deliberate / is the missing dispatch-site model override an uncompensated defect / is RP-MODEL-01 subagent model omission fleet-conformant / what does TRDD-G5YIPLBD decide about agents model pins / is an unkept commit-message promise automatically a defect / does role-plugins-spec RULED 2026-08-08 apply to main agents or subagents / is the ruling on the governance-rules branch visible from origin HEAD / primary spec versus secondary description of a spec ruling / what would a third party cloning this repo actually see"
ocd: 2026-06-16
lmd: 2026-08-29
metadata:
  node_type: memory
  type: project
  tier: hub
  functionality: architecture
  globs: ["agents/**", "skills/**", "scripts/**", "commands/**", "hooks/**", "lib/**"]
publish-globally: false
---
ai-maestro-architect-agent (AMAA) is the **ARCHITECT** role plugin of the AI Maestro fleet: one architect per project, owning technical architecture design, requirements analysis, API research, the design-document lifecycle, and complete implementation handoffs to the orchestrator. It does not write production code — it produces specifications and design artifacts.

## Parts map
- **agents/** — the main agent (`ai-maestro-architect-agent-main-agent`) + LOCAL HELPER sub-agents (documentation-writer, modularizer-expert, planner, api-researcher, cicd-designer). Sub-agents are AMP-restricted (only the main agent messages other agents). No agent carries a `model:` key — each sub-agent inherits the invoking session's model (CPV CA-04); force `opus` at the dispatch call site when one genuinely needs it.[^1]
- **skills/** — the `amaa-*` capability skills: design-lifecycle, design-management, requirements-analysis, planning-patterns, cicd-design, hypothesis-verification, github-integration, session-memory (transcript/session restore — distinct from the wiki memory). The design state machine + redesign loop live in `scripts/amaa_design_lifecycle.py`.
- **AI-Maestro kanban integration** (TRDD-364ccafc, architect#7) — on design **approval** (design-lifecycle PROCEDURE 3) the architect creates an AI-Maestro **server** `epic` task + first-level children via `amp-kanban-create-task --task-type epic … --parent` (op-create-kanban-epic), carries the epic id in the design-handoff as `aimaestro_task_id`, and reads child progress via `amp-kanban-list` (op-query-kanban-progress) — all through R23 frozen verbs.[^3]
- **design/** — the 3-pillars artifacts: `requirements/PRRD.md` (project rules) + the 4 kanban zones `tasks/ proposals/ refused/ archived/` (the LOCAL design-doc/TRDD zones — distinct from the AI-Maestro server kanban above).
- **hooks/** — a `Stop` hook (`scripts/amaa_stop_check.py`) that blocks exit until design work is complete (modern `args` exec form).
- **scripts/** — the design/lifecycle/github/publish tooling; `publish.py` is the CPV-canonical strict pipeline (no-skip, process-ancestry pre-push guard). This plugin is the CPV **`remote-validation` profile** (de-vendored validators, drives the remote `cpv-remote-validate --strict` gate); `plugin.json` declares its by-design canon divergences via `cpv.pipeline.intentional_divergence` — **never run CPV `--force-templates` here, it damages the profile.**[^2]
- Memory: AMAA uses the GLOBAL janitor memory system (see the project `CLAUDE.md` "## Memory"); it ships NO per-plugin memory-recall/write skills.


^ATOM-S4SF-VMIJ [desc: "No agent carries a model: key; MAIN agents omitting it is the RULED fleet state (RP-MODEL-01, ai-maestro#136, 2026-08-08); subagent omission is PERMITTED, not prescribed (TRDD-G5YIPLBD).", keywords: does_this_agent_pin_a_model why_no_model_field_in_agent_frontmatter agent_model_tier_alias_removed which_model_does_the_planner_use_now cache-warmth_invariant_CPV_CA-04 model_alias_vs_pinned_deprecated_id sub-agent_inherits_invoking_session_model dispatch-site_model_override_missing opus_sonnet_frontmatter_pins_deleted commit_dce503e_model_pins_deleted is_the_model_pin_still_deliberate planner_cheaper_model_still_on_sonnet is_the_missing_dispatch-site_override_a_defect RULED_2026-08-08_main_agents_omit_model TRDD-G5YIPLBD_decision_on_agents_model_pins spec_version_1.2.0_role-plugins-spec subagent_model_pinning_deliberately_open governance-rules_branch_vs_origin_HEAD, ocd: 2026-08-29, lmd: 2026-08-29]
Verified 2026-08-29 by reading the YAML frontmatter of all 6 `agents/*.md` files directly: none carries a `model:` key — each carries only `name`, `description`, `skills`. So every sub-agent inherits the invoking session's model, not a pinned tier. Previously this page said (2026-06-16): "Each agent's `model:` is a **deliberate alias** (`opus`; `sonnet` for the cheaper planner) — not a pinned deprecated model ID." — that was TRUE when written. Commit `dce503e` (2026-07-03, "perf(tokens): fix cache-warmth pins, broken tldr cmds, unbounded cat recipes") deliberately DELETED all 6 `model:` pins (`opus` x5, `sonnet` x1). The commit message promised a compensating dispatch-site override; verified 2026-08-29 that a grep for one across `agents/`, `skills/`, `scripts/` returns zero hits, so the planner (previously pinned `sonnet`) now inherits whatever model the invoking session runs — a real cost difference from the pin it used to carry, but NOT a conformance gap. CORRECTED 2026-08-29 from the PRIMARY source (`role-plugins-spec.md`, `spec-version 1.2.0`, `status: authoritative`, branch `governance-rules` — absent from `origin/HEAD`): `RP-MODEL-01` **RULED 2026-08-08** (`ai-maestro#136`, closing `TRDD-TYB3Q1NJ`) that role-plugin **MAIN agents OMIT `model:`** entirely — AMAA's main agent already carries no `model:` key, which is the RULED fleet state ahead of the on-next-release migration deadline, not a deviation (`ai-maestro-assistant-role-agent`'s main agent is graded the same way: "conformant with the RULED policy"). Subagent `model:` policy is deliberately OPEN, not ruled — 15 subagents are pinned fleet-wide (measured 2026-08-08: integrator x10, orchestrator x5), so AMAA's 5 subagents carrying no key is PERMITTED, not prescribed; restoring the planner's `sonnet` pin remains available to the owner at any time. The spec's own pre-ruling historical record (measured table: `opus` x4, no key x2 including AMAA+programmer, `inherit` x1, `sonnet` x1) cites `ai-maestro-autonomous-agent` (`RP-TITLES-02`-mandatory) pinning `sonnet` as the decisive counterexample that broke the old "universal opus pin" claim and motivated the ruling. `TRDD-G5YIPLBD` records the decision: change nothing in `agents/`. [^4] [^5] [^6] [^7] [^8]

## Applies to
- [[decouple-api-to-cli]] — skills call the frozen CLI verbs (`amp-*`, `aimaestro-*.sh`), never raw `/api/*` routes.

## See also
- (lateral links to other functionality hubs, once they exist)

## Notes and lessons learned
[^1]: [id:ATOM-6RR1-WSMA, status:superseded, superseded-by:ATOM-JJTA-I5UN, keywords:"should I strip the model field from my agents, CPV best practice omit model, changelog says deprecated model IDs, does the deprecated-model warning apply to bare aliases, read the changelog and update the plugin, why is there no model field now, did dce503e remove the pins, is this lesson still true", ocd:2026-06-20, lmd:2026-08-29] Claude Code changelog audit (through **2.1.183**) vs this plugin (2026-06-20): **NO required update.** The 6 agent `model:` fields are bare aliases (`opus` ×5, `sonnet` ×1) that auto-resolve to the current generation — NOT pinned deprecated IDs — so the 2.1.183 "deprecated/auto-updated model" warning (which targets pinned *old* IDs in agent frontmatter) does not apply, and a "CPV best-practice: omit `model:`" or "changelog update" pass must NOT strip them: doing so would silently change each sub-agent's model tier (the planner is on `sonnet` on purpose, for cheaper planning). Also verified current: `hooks/hooks.json` is the modern Stop-only `args`-exec form whose `amaa_stop_check.py` reads stdin JSON (the live hook contract); no `.mcp.json` is shipped (MCP-config changelog items N/A). Every other plugin-relevant changelog item is ADDITIVE/optional, not breaking — candidate future enhancements (not adopted): Stop/SubagentStop `hookSpecificOutput.additionalContext` (2.1.x), skill/command `disallowed-tools` frontmatter, the `MessageDisplay` hook event, `plugin.json` `defaultEnabled`. Lesson: a "read the changelog and update the plugin" request usually resolves to a VERIFICATION (most CC entries are runtime/TUI fixes); audit `model:`-frontmatter / `hooks.json` shape / `.mcp.json` against the relevant subset and report "current" rather than inventing churn. UPDATE 2026-08-29: this prediction came true — commit `dce503e` (2026-07-03) deliberately deleted all 6 `model:` pins 13 days later, on purpose (not a stripping mistake). See [^4] for the still-transferable lesson: the compensating dispatch-site override that commit promised was never added, and this guardrail was left `status:valid` for 57 days, silently defending a state that no longer existed.
[^2]: [id:ATOM-60U1-STH4, status:valid, keywords:"CPV standardize rewrote my publish.py, force-templates re-vendored files, RC-PIPELINE-DRIFT-001 warnings, how do I stop CPV pipeline drift warnings, intentional_divergence selector not suppressor", ocd:2026-06-22, lmd:2026-06-22] CPV canonical-pipeline upgrade (architect#23 / fleet ai-maestro#44), shipped v2.10.2 (commit `b0b735d`). This plugin is the CPV **`remote-validation` profile** by design: it de-vendored the CPV validators, `publish.py`/CI drive the REMOTE `cpv-remote-validate --strict` gate, and `.githooks/pre-push` is a process-ancestry gate (a stronger alternative to the env-var gate). Old CPV 2.137.0's `standardize --fix --force-templates` DAMAGED it (re-vendored publish.py 62→73KB, re-created `cpv_network_resilience.py`, added a competing env-gate pre-push, downgraded `notify-marketplace.yml`) — sandbox-proven, then reverted. The correct upgrade with CPV ≥ 2.143.0 (profile-aware): declare each by-design-divergent shared-canon file in `plugin.json` → `cpv.pipeline.intentional_divergence` (a SELECTOR not a suppressor, TRDD-02e1672b — drift becomes an auditable INFO, never silenced, and `--force-templates` skips it). The 2 residual `RC-PIPELINE-DRIFT-001` WARNINGs (`publish.py` by-design, `notify-marketplace.yml` ahead-of-canon) are advisory + non-blocking + EXPECTED. Lesson: NEVER run CPV `--force-templates` on this plugin; bring it to canon by editing `intentional_divergence` in the manifest and let `--strict` (0 C/M/MI/N + advisory WARNINGs only) be the gate.
[^3]: [id:ATOM-EPB3-OBC9, status:valid, keywords:"where is the kanban automation code, Multiple AMP agents found, which procedure wires the kanban ops, kanban round-trip cannot run in a dev session, frozen CLI feature plugin side is op-docs", ocd:2026-06-22, lmd:2026-06-22] architect#7 kanban integration (TRDD-364ccafc). The architect-side is markdown OPS (`op-create-kanban-epic` / `op-query-kanban-progress`, each in the skill + its `-ops` twin) the agent executes — there is NO separate automation code; "wiring" = referencing the ops from design-lifecycle `procedures.md` PROCEDURE 3 (approve→handoff) + PROCEDURE 4 (track), NOT PROCEDURE 5 (which is complete+archive, post-implementation — a wrong assumption I made then corrected by READING procedures.md). LIVE use is DEPLOYMENT-time: a dev session has no AMP agent binding (no `$AID`/`$AMP_CONFIG`, `~/.ai-maestro` empty), so `amp-kanban-create-task`/`list` can't resolve a single agent here ("Multiple AMP agents found") — a real AMCOS-spawned architect+team runs + verifies the round-trip. Two CLI gaps remain upstream (no `--attachments` on create, no `--parent` filter on list) — both with architect-side workarounds (design-doc path in `--description`; client-side `jq` `parentTask` filter). Lesson: for a frozen-CLI-backed feature, the plugin side is op-docs + lifecycle wiring; confirm the wiring hooks the RIGHT lifecycle state by reading the procedure list (don't assume), and defer the live round-trip to deployment.
[^4]: [id: ATOM-JJTA-I5UN, status: valid, keywords: "two_part_change_only_half_done second_half_never_executed dispatch_site_model_override_missing guardrail_left_status_valid_after_prediction_came_true stale_lesson_misleads_future_session model_pin_removed_but_not_replaced cache_warmth_invariant_compensating_action verify_a_fix_actually_completed_both_halves lesson_prediction_came_true_unflagged check_dependent_lessons_after_a_design_change", ocd: 2026-08-29, lmd: 2026-08-29] DO NOT assume a two-part change is fully applied once the first half lands, BECAUSE dce503e (2026-07-03) deleted the model: pins but never added the promised dispatch-site opus override — the planner silently lost its cheaper-model guarantee, and the guardrail lesson that predicted this was left status:valid for 57 days, actively misleading. DO grep for the compensating half (here: a dispatch-site model: override across agents/, skills/, scripts/) before trusting a partial fix, and re-check any lesson that predicted the risk.
[^5]: [id: ATOM-XNG7-YJWO, status: valid, supersedes: ATOM-S4SF-VMIJ, desc: "fleet-conformant per RP-MODEL-01; TRDD-G5YIPLBD decided to change nothing in agents/", keywords: "does_this_agent_pin_a_model is_the_missing_dispatch-site_override_a_defect RP-MODEL-01_subagent_model_omission fleet_conformant_model_pin_rule TRDD-G5YIPLBD_decision cache-warmth_invariant_CPV_CA-04 role-plugins-spec_RP-MODEL-01 ai-maestro-autonomous-agent_pins_sonnet_counterexample fleet_survey_model_pin_distribution subagent_omits_model_key_by_design is_the_model_pin_still_deliberate", ocd: 2026-08-29, lmd: 2026-08-29] DO NOT frame AMAA's missing dispatch-site model override as an uncompensated gap, BECAUSE role-plugins-spec.md's RP-MODEL-01 (spec-version 1.0.0, status: authoritative) prescribes exactly this state for subagents — pin the family alias `opus` on the MAIN agent, OMIT it on SUBagents — explicitly conceding this contradicts CPV CA-04 cache-warmth guidance; a fleet survey (TRDD-20260807_183235+0200-OH3N6OXJ) found only half the fleet follows even the main-agent clause, and `ai-maestro-autonomous-agent` pins `sonnet` while the same spec's RP-TITLES-02 calls that plugin MANDATORY — a universal rule whose own required counterexample was never true. DO cite `TRDD-G5YIPLBD` (decision: change nothing in `agents/`) instead of flagging a defect. SUPERSEDED BODY: Verified 2026-08-29 by reading the YAML frontmatter of all 6 `agents/*.md` files directly: none carries a `model:` key — each carries only `name`, `description`, `skills`. So every sub-agent now inherits the invoking session's model, not a pinned tier. Previously this page said (2026-06-16): "Each agent's `model:` is a **deliberate alias** (`opus`; `sonnet` for the cheaper planner) — not a pinned deprecated model ID." — that was TRUE when written. Commit `dce503e` (2026-07-03, "perf(tokens): fix cache-warmth pins, broken tldr cmds, unbounded cat recipes") deliberately DELETED all 6 `model:` pins (`opus` x5, `sonnet` x1) so every sub-agent inherits the invoking session's model — the cache-warmth invariant, matching the ecosystem's CPV CA-04. The commit message promised the compensating half: "force opus at the dispatch call site when an agent genuinely needs it, not in the definition" — verified 2026-08-29 that this was NEVER done: a grep for a dispatch-site `model:` override across `agents/`, `skills/`, `scripts/` returns zero hits. So the planner (previously pinned `sonnet` for cheaper planning) now silently inherits whatever model the invoking session runs.
[^6]: [id: ATOM-6O45-9SY8, status: valid, desc: "the inverse of [^4]: an unkept promise can be superseded, not abandoned — check the spec first", keywords: "does_this_agent_pin_a_model unkept_commit_message_promise_is_not_automatically_a_defect superseded_intent_vs_abandoned_intent check_authoritative_spec_before_escalating_a_gap cache-warmth_invariant_CPV_CA-04 is_the_model_pin_still_deliberate RP-MODEL-01_subagent_model_omission fleet_spec_settles_end_state commit_promise_never_fulfilled_but_not_a_bug two-part_change_verification_inverse_lesson", ocd: 2026-08-29, lmd: 2026-08-29] DO NOT treat an unkept promise in a commit message as automatically a defect, BECAUSE the intent may have been SUPERSEDED rather than abandoned — dce503e's second half was never needed once the fleet spec wanted subagents unpinned (see [^5], RP-MODEL-01). DO check whether an authoritative spec already settles the end state before escalating a gap.
[^7]: [id: ATOM-975R-4OUK, status: valid, supersedes: ATOM-S4SF-VMIJ, desc: "primary spec 1.2.0: MAIN agents OMIT model: (RULED, ai-maestro#136); subagent pinning is OPEN, not prescribed", keywords: "does_this_agent_pin_a_model is_the_missing_dispatch-site_override_a_defect RULED_2026-08-08_main_agents_omit_model ai-maestro#136_TRDD-TYB3Q1NJ spec_version_1.2.0_role-plugins-spec subagent_model_pinning_deliberately_open is_RP-MODEL-01_subagent_model_omission_fleet-conformant TRDD-G5YIPLBD_decision_on_agents_model_pins governance-rules_branch_vs_origin_HEAD primary_spec_versus_secondary_description cache-warmth_invariant_CPV_CA-04 is_the_model_pin_still_deliberate", ocd: 2026-08-29, lmd: 2026-08-29] DO NOT cite a secondary description of a spec ruling as though it were the ruling, BECAUSE `role-plugins-spec.md` (primary, `spec-version: 1.2.0`, `status: authoritative`, on branch `governance-rules`) RULED 2026-08-08 (`ai-maestro#136`, closing `TRDD-TYB3Q1NJ`) that role-plugin MAIN agents OMIT `model:` entirely — not "pin `opus` on the main agent, omit on subagents" as [^5] stated (sourced from a secondary TRDD description read via a version-pinned plugin cache, itself amended twice, and misquoted the spec-version as 1.0.0). Subagent `model:` policy is deliberately OPEN, not ruled to omit — 15 subagents are pinned fleet-wide (measured 2026-08-08: integrator x10, orchestrator x5). DO cite the primary spec directly: AMAA's MAIN agent already carrying no `model:` key is the RULED fleet state, ahead of the on-next-release migration deadline (`ai-maestro-assistant-role-agent` is graded the same way: "conformant with the RULED policy"); AMAA's subagents omitting is PERMITTED, not prescribed — restoring the planner's `sonnet` pin remains available to the owner at any time. NOTE the ruling's own scope: it is present on remote branch `governance-rules` and absent from `origin/HEAD`, which has never carried this file — published where governance specs are published, invisible to a mainline reader. SUPERSEDED BODY: Verified 2026-08-29 by reading the YAML frontmatter of all 6 `agents/*.md` files directly: none carries a `model:` key — each carries only `name`, `description`, `skills`. So every sub-agent inherits the invoking session's model, not a pinned tier. Previously this page said (2026-06-16): "Each agent's `model:` is a **deliberate alias** (`opus`; `sonnet` for the cheaper planner) — not a pinned deprecated model ID." — that was TRUE when written. Commit `dce503e` (2026-07-03, "perf(tokens): fix cache-warmth pins, broken tldr cmds, unbounded cat recipes") deliberately DELETED all 6 `model:` pins (`opus` x5, `sonnet` x1). The commit message promised a compensating dispatch-site override; verified 2026-08-29 that a grep for one across `agents/`, `skills/`, `scripts/` returns zero hits, so the planner (previously pinned `sonnet`) now inherits whatever model the invoking session runs — a real cost difference from the pin it used to carry, but NOT a conformance gap. `role-plugins-spec.md` (spec-version 1.0.0, status: authoritative) RP-MODEL-01 prescribes pinning `opus` on the MAIN agent only and OMITTING it on subagents, explicitly conceding this contradicts CPV CA-04's cache-warmth guidance — so AMAA's 5 helper sub-agents carrying no `model:` key is fleet-conformant. The main-agent clause is the part AMAA does not follow, and it is itself unenforceable: a fleet survey (`TRDD-20260807_183235+0200-OH3N6OXJ`) found 4 plugins pinning `opus`, 2 with no key (architect=AMAA, programmer), 1 `inherit`, 1 `sonnet` — and `ai-maestro-autonomous-agent` pins `sonnet` while the SAME spec's RP-TITLES-02 calls that plugin MANDATORY, so a universal whose own required counterexample was never true. `TRDD-G5YIPLBD` records the decision: amend the rule to describe reality, change nothing in `agents/`. The remedy, if the cost is ever unwanted, is choosing the model at the dispatch call site (`executor.py` already carries this as advisory guidance) — an owner cost decision, not a defect.
[^8]: [id: ATOM-8IWW-V03O, status: valid, desc: "extends [^6]: the inverse lesson was itself built the same wrong way — read the primary, and what a clone actually sees", keywords: "does_this_agent_pin_a_model primary_source_versus_secondary_description cached_plugin_version_dated_by_construction check_what_a_third_party_would_clone governance-rules_branch_vs_origin_HEAD is_the_model_pin_still_deliberate RULED_2026-08-08_main_agents_omit_model cache-warmth_invariant_CPV_CA-04 TRDD-G5YIPLBD_decision_on_agents_model_pins is_an_unkept_commit-message_promise_automatically_a_defect", ocd: 2026-08-29, lmd: 2026-08-29] DO NOT trust a document that DESCRIBES a spec rule as though it were the rule itself, BECAUSE [^6]'s own inverse lesson was itself built the same way: it was drafted from another project's TRDD describing role-plugins-spec.md, read out of one arbitrarily-chosen directory among several cached plugin versions, while the primary spec sat on disk unread and two amendments newer — and even the primary is only half a fix when it is a local checkout, since this ruling lives on remote branch `governance-rules` and is absent from `origin/HEAD`. DO read the primary source before publishing a claim about what a spec rules, and check what a third party cloning the repo would actually see.
