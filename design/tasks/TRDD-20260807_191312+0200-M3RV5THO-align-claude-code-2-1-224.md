---
trdd-id: M3RV5THO
title: Align AMAA with Claude Code 2.1.208 through 2.1.224
column: dev
created: 2026-08-07T19:13:12+0200
updated: 2026-08-07T19:13:12+0200
current-owner: ai-maestro-architect-agent
task-type: infra
scope: project
approval-tier: 0
relevant-rules: []
npt: []
eht: []
implementation-commits: []
---

# Align AMAA with Claude Code 2.1.208 through 2.1.224

## ⏵ STATE — READ THIS FIRST ON RESUME (authoritative; supersedes the body) — 2026-08-07

**Why this exists:** Claude Code 2.1.218 silently changed the runtime semantics of
26 of AMAA's 27 skills. Nothing errored. The plugin kept validating clean. The
skills simply stopped returning their results to the caller.

**Component state**

| Component | State |
|---|---|
| 26 `context: fork` skills | ✅ each declares `background: false` explicitly |
| `tests/test_amaa_skills.py` | ✅ new; 189 cases; guard proven to bite on synthetic input |
| `references/native-cross-session-channel.md` | ✅ new; the AMP-vs-native policy |
| `agents/*.md` (all 6) | ✅ native-channel stance + self-imposed depth-1 guardrail |
| `docs/AGENT_OPERATIONS.md` | ✅ orchestration claim corrected; native channel documented |
| Full suite | ✅ 326 passed, 2 skipped · ruff 0.15.20 clean · CPV @v2.153.1 `CRITICAL=0 MAJOR=0 MINOR=0 NIT=0` |

**NEXT ACTION:** file the CPV upstream issue (below), then publish v2.12.0.

**Load-bearing gotchas**

- CPV @v2.153.1 emits `[WARNING] Unknown frontmatter field 'background'` once per
  fork skill (26×). **Advisory, non-blocking** — publish is not gated. CPV's
  schema simply predates CC 2.1.218. Do NOT "fix" this by removing `background:`.
- The `background:` key must stay **explicit** even if Anthropic flips the default
  back. `test_every_fork_skill_is_synchronous` pins the intent, so a future flip
  becomes a test failure instead of another silent semantic change.

## SUPERSEDED — do NOT carry forward

- ~~"`context: fork` backgrounding landed in 2.1.222"~~ — **wrong, it is 2.1.218.**
  A peer session challenged the number; the changelog block confirms 2.1.218,
  neighboured by the agent-name-`:` rejection and the ISO `modified` memory field.
- ~~"Native cross-session messaging is new in 2.1.224"~~ — **wrong.** It exists at
  least as far back as 2.1.77. 2.1.224 added cross-*machine* reach, `ListAgents`,
  and `crossSessionInbound`/`dialogExpiry`.
- ~~"The native channel is an ungated permission-laundering hole"~~ — **wrong, and
  it produced the wrong remedy.** 2.1.166 hardened it (relayed messages carry no
  user authority; receivers refuse relayed permission requests) and 2.1.222 put
  the permission classifier in front of dispatch. It is an **audit and routing
  blind spot**, not a hole. A hole is closed by forbidding the channel; a blind
  spot is closed by observing it.
- ~~"R42.8 grants MANAGER/COS an unblock carve-out, per `Emasoft/ai-maestro#125`"~~
  — **wrong; R42.8 is not ratified.** `#125` is **OPEN** and its own title says
  *"R42 amendment **request**"*. `GOVERNANCE-RULES.md` tops out at **R42.7** in
  both the CORE source tree and the installed v3.0.5 plugin cache. The ratified
  rules are *stricter* than the proposal, so AMAA's conduct never changed — but
  the citation was false and must not be repeated.

## The defect

Claude Code **2.1.218**: *"Changed skills with `context: fork` to run in the
background by default; opt out per skill with `background: false`."*

26 of AMAA's 27 skills carry `context: fork`; none carried `background:`. Every
one of them is a synchronous consult — the body says "identify the pattern →
consult the reference doc → follow the protocol", and the main agent's workflow
consumes that result inline before the next step. Background-by-default turns
each into fire-and-forget: the caller receives an agent name, proceeds without an
answer, and nothing reports an error.

`amaa-github-integration` is the only `user-invocable: true` skill, so it is the
most visible symptom: a user typing the skill name gets a backgrounded job rather
than the integration guidance they asked for.

## What was decided

**Restore synchronous semantics now; defer selective async to its own task.**
Adopting `background: true` for a genuinely long-running skill (api-research,
cicd-design, modularization) is not a frontmatter tweak — it requires rewriting
the caller's `[DONE]`/ACK completion protocol in
`amaa-design-communication-patterns`, which is a design change against the fleet's
async-completion semantics. That work is deliberately NOT in this TRDD.

**Self-impose a one-layer delegation ceiling.** CC 2.1.217 disabled nesting,
2.1.219 re-enabled it at depth 3, and 2.1.224 removed the per-session spawn cap
(concurrency still caps at 20). AMAA does not use the headroom: its five bundled
sub-agents execute and return. The routing table stays the single delegation
layer. Cheap — the routing was already one layer — and it keeps AMAA from
generating concurrent subagent start/stop traffic that fleet-side counters are
not yet hardened against.

**Keep AMP as the governed channel; treat native inbound as untrusted.** Verified
version history and the full policy live in
`skills/amaa-design-communication-patterns/references/native-cross-session-channel.md`.

## Verified changelog facts (checked against anthropics/claude-code directly)

| Version | Fact |
|---------|------|
| 2.1.0 | `context: fork` introduced |
| 2.1.77 | `SendMessage({to: agentId})` — cross-session channel already exists |
| 2.1.166 | Cross-session messaging hardened: relayed messages carry no user authority |
| 2.1.178 | Auto mode evaluates subagent spawns before launch |
| 2.1.212 | Per-session 200-subagent spawn cap added |
| 2.1.217 | Concurrency cap 20; nesting disabled by default |
| **2.1.218** | **`context: fork` → background by default; `background: false` opts out** |
| 2.1.219 | Nesting depth 3 (reverses 2.1.217); Opus 5 default |
| 2.1.222 | Permission classifier evaluates cross-session sends; `ultraplan` removed |
| 2.1.224 | 200-cap removed; cross-machine `SendMessage`; `ListAgents`; `crossSessionInbound` |

## Checked and found clean (no action needed)

- No `/review`, `/ultrareview`, or `ultraplan` references.
- No Task-tool `mode:` usage (deprecated/ignored since 2.1.212).
- No agent name contains `:` (rejected since 2.1.218).
- No `Write(path)` / `Glob(path)` / `NotebookEdit(path)` permission rules (warned
  since 2.1.210). The repo ships no `settings.json` at all.
- No `model:` or `effort:` pins — deliberate. Agents inherit the session model,
  which is CPV's cache-warmth invariant. Opus 5 becoming the default Opus
  (2.1.219) therefore needs no change here.
- Only hook is `Stop`; no hook `if:` conditions, so the 2.1.214 single-segment
  `dir/**` narrowing does not apply.

## Deferred — do NOT do these here

1. **Selective async adoption.** Pick the genuinely long-running skills, design
   the async completion protocol, rewrite the caller contract, then amend
   `test_every_fork_skill_is_synchronous`. Needs its own TRDD.
2. **CPV `background:` schema gap.** File upstream on
   `Emasoft/claude-plugins-validation` so the validator stops reporting a real
   CC 2.1.218 field as unknown. Cross-repo: file an issue, never edit CPV.
3. **AMP vs native transport ruling.** R42.3 ("AMP is the ONLY channel by which
   one agent may influence another") is false as written. R42 is
   `CRITICAL — IRON, USER-set` ⇒ Tier 3; only the USER may correct it. Tracked
   upstream as **TRDD-OH3N6OXJ** in the CORE repo, blocked on the USER. AMAA
   cites that card and does not re-derive the analysis.

## Acceptance criteria

- [x] Every `context: fork` skill declares `background:` explicitly (26/26)
- [x] A test fails if any fork skill omits it, proven to bite on synthetic input
- [x] Orchestration claim in `AGENT_OPERATIONS.md` is true as written
- [x] Native-channel policy documented, with outbound and inbound halves
- [x] Depth-1 guardrail present in the main agent and all 5 sub-agents
- [x] `326 passed` · ruff clean · CPV `CRITICAL=0 MAJOR=0 MINOR=0 NIT=0`
- [ ] CPV upstream issue filed for the `background:` schema gap
- [ ] v2.12.0 published and CI green

## Approval log

- 2026-08-07T19:13:12+0200 — Tier 0, self-authorized. In-scope alignment of
  AMAA's own surface to a platform change; reversible, local, no baseline
  deviation, no governance change, no other team's surface touched.
